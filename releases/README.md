# EMPIRE AT WAR Launcher release feed

`compileRelease.bat` generates `LauncherUpdateData.xml` and the updater files
in this directory. Keep the generated metadata at the directory root and the
files inside `Stable`, `Beta`, or `Test` according to their release channel.
